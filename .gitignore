# dependencies
node_modules/
.pnpm-store/

# build output
dist/
lib/
*.tsbuildinfo

# @fusionui/tokens ships its generated outputs (single source of truth → css/scss/
# ts/native). Committed so consumers and CI/deploy need no extra build step.
!packages/tokens/dist/
!packages/tokens/dist/**

# test / coverage
coverage/
.nyc_output/

# logs
*.log
npm-debug.log*
pnpm-debug.log*

# editor / OS
.DS_Store
Thumbs.db
.idea/
.vscode/*
!.vscode/extensions.json

# local env
.env
.env.local

# research reference clones (kept locally, not part of the monorepo)
vuetify/
vuesax/
vuesax-next/

# unrelated repos that happen to live in this working dir
GLSL/
depot_tools/
skia/
threejs-skills/
extras/
FusionUI.txt
plans/
CLAUDE.md
.claude/
