---
'@rukkiecodes/native': minor
---

Add the Badge component — a compact status / label chip with colour variants (default, success, warning, error, pending, notifications), three sizes, a full radius scale, and an optional leading icon.
