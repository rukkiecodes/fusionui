---
'@rukkiecodes/native': minor
---

Add the Aurora component — a Skia + Reanimated aurora shader (flowing light bands over a night-sky gradient, with configurable colours, speed, intensity and drift). Also organise the registry into categories (Typography, Actions, Effects).
