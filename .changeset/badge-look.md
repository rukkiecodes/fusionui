---
'@rukkiecodes/native': patch
---

Badge: make it actually look like a badge — small pill by default (semibold, tighter type and padding), a neutral grey `default` variant instead of the old green, and a `notifications` outline that's visible on light backgrounds.
